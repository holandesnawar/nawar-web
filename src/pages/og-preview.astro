---
// Página de preview de la imagen Open Graph (1200×630).
// Abre /og-preview en Chrome → DevTools → modo dispositivo 1200x630 →
// capture full-size screenshot → guardar como public/og-image.png
---
<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="robots" content="noindex" />
  <title>OG Image Preview</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link
    href="https://fonts.googleapis.com/css2?family=Poppins:wght@600;700;800&display=swap"
    rel="stylesheet"
  />
  <style>
    * { box-sizing: border-box; margin: 0; padding: 0; }
    html, body { background: #0a0043; }
    body {
      display: flex;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
    }

    /* OG canvas exacto: 1200×630 */
    .og {
      position: relative;
      width: 1200px;
      height: 630px;
      background: #1D0084;
      overflow: hidden;
      font-family: 'Poppins', system-ui, sans-serif;
    }

    /* Halo radial suave desde el centro */
    .og-glow {
      position: absolute;
      inset: 0;
      background: radial-gradient(ellipse at center, rgba(77,163,255,0.20) 0%, transparent 60%);
      pointer-events: none;
    }

    /* Semicirculos grandes que:
       1) tocan las 4 esquinas del canvas (0,0)(0,630) y (1200,0)(1200,630)
       2) pero solo asoman un 10% de su diametro (bulge ~105px)
       Geometria: r=525, centro en (-420, 315) → radio² = 420² + 315²
       Bounding box 1050x1050, 945px off-screen, 105px visibles */
    .og-orb {
      position: absolute;
      top: -210px; /* cy(315) - r(525) */
      width: 1050px;
      height: 1050px;
      border-radius: 50%;
      pointer-events: none;
      background: radial-gradient(
        circle at 100% 50%,
        rgba(77,163,255,0.60) 0%,
        rgba(77,163,255,0.42) 40%,
        rgba(77,163,255,0.25) 75%,
        rgba(77,163,255,0.12) 100%
      );
      border: 2px solid rgba(77,163,255,0.65);
      filter:
        drop-shadow(0 0 60px rgba(77,163,255,0.35))
        drop-shadow(0 20px 40px rgba(11,109,240,0.25));
    }
    .og-orb-left  { left: -945px; }
    .og-orb-right {
      right: -945px;
      background: radial-gradient(
        circle at 0% 50%,
        rgba(77,163,255,0.60) 0%,
        rgba(77,163,255,0.42) 40%,
        rgba(77,163,255,0.25) 75%,
        rgba(77,163,255,0.12) 100%
      );
    }

    /* Contenido centrado */
    .og-text-wrap {
      position: absolute;
      inset: 0;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      padding: 60px 60px 140px;
      text-align: center;
      z-index: 2;
    }
    .og-title {
      font-weight: 700;
      line-height: 1.2;
      letter-spacing: -0.02em;
      white-space: nowrap;
    }
    .og-title-white {
      font-size: 54px;
      background: linear-gradient(180deg, #ffffff 0%, rgba(255,255,255,0.62) 100%);
      -webkit-background-clip: text;
      background-clip: text;
      -webkit-text-fill-color: transparent;
      display: block;
    }
    .og-title-blue {
      font-size: 54px;
      color: #4da3ff;
      display: block;
      margin-top: 10px;
    }

    /* Logo centrado abajo */
    .og-logo {
      position: absolute;
      left: 50%;
      bottom: 60px;
      transform: translateX(-50%);
      height: 56px;
      width: auto;
      filter: drop-shadow(0 4px 18px rgba(77,163,255,0.4));
      z-index: 2;
    }
  </style>
</head>
<body>
  <div class="og">
    <div class="og-glow"></div>

    <!-- Semicirculos tocando las esquinas -->
    <div class="og-orb og-orb-left"></div>
    <div class="og-orb og-orb-right"></div>

    <!-- Texto en EXACTAMENTE 2 lineas -->
    <div class="og-text-wrap">
      <div class="og-title">
        <span class="og-title-white">Descubre el primer método</span>
        <span class="og-title-blue">creado para hispanohablantes</span>
      </div>
    </div>

    <!-- Logo Nawar -->
    <img
      src="https://d1yei2z3i6k35z.cloudfront.net/9533860/671a9c9265e23_Logo_Nawar_2.png"
      alt="Holandés Nawar"
      class="og-logo"
    />
  </div>
</body>
</html>
